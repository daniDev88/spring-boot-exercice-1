spring:
  application:
    name: ec-ms-rates-boot
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

---
spring:
  config:
    activate:
      on-profile: local
  cloud:
    config:
      enabled: false